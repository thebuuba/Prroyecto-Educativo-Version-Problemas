# `@aulabase/database`

Paquete reservado para contratos de base de datos, helpers SQL y documentación de acceso a datos.

Los SQL ejecutables viven en `supabase/` para mantener compatibilidad con Supabase PostgreSQL.
