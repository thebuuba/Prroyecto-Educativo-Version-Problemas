{
  "name": "edugest-main",
  "version": "1.0.0",
  "description": "EduGest Modernized with Vite and ES Modules",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "assemble": "./scripts/assemble-all.sh",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build && npm --prefix server run check",
    "backend:check": "npm --prefix server run check",
    "backend:smoke": "cd server && npm run smoke",
    "deploy": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  }
}
