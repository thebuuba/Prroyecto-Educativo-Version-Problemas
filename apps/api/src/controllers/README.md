# Controllers

Orquestan request/response HTTP y delegan lógica a `services/`.

Estado actual: las rutas legacy en `routes/` aún contienen lógica de controlador. La migración debe hacerse endpoint por endpoint para evitar regresiones.
