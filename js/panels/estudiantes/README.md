# Adaptador Legacy: Estudiantes

La fuente real vive en `apps/web/src/panels/estudiantes/`.

Esta carpeta conserva las rutas publicas legacy (`/js/panels/estudiantes/**`) como reexports temporales para lazy loading, imports historicos y compatibilidad con el renderer dinamico.
