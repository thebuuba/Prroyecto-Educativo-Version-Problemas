{
  "name": "aulabase-sql-api-compat",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ../apps/api run dev",
    "start": "npm --prefix ../apps/api run start",
    "migrate": "npm --prefix ../apps/api run migrate",
    "check": "npm --prefix ../apps/api run check",
    "smoke": "npm --prefix ../apps/api run smoke"
  }
}
