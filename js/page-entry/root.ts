// Adaptador temporal: la entrada real del frontend vive en apps/web/.
// Mantener este archivo evita romper imports existentes durante la migración gradual.
import '../../apps/web/src/page-entry/root.ts';
